import { ToolDefinition } from "../core/registry";
import { bridgeSendAsToolResult } from "../core/bridgeToolResult";
import { z } from "zod";
const ueUiCreateWidget: ToolDefinition = { id: "ue_ui_create_widget", name: "Create Widget Blueprint", description: "Create a new UMG Widget Blueprint", descriptionJa: "新しいUMG Widget Blueprintを作成", category: "ue_ui", inputSchema: z.object({ name: z.string(), path: z.string().default("/Game/UI") }), handler: async (p) => { return bridgeSendAsToolResult("unreal", "UiCreateWidget", p, { successMessage: (_, p) => `Widget created: ${p.name}` }) } };

const ueUiAddText: ToolDefinition = { id: "ue_ui_add_text", name: "Add Text Block", description: "Add a TextBlock to a widget", descriptionJa: "ウィジェットにTextBlockを追加", category: "ue_ui", inputSchema: z.object({ widgetName: z.string(), text: z.string(), fontSize: z.number().default(24) }), handler: async (p) => { return bridgeSendAsToolResult("unreal", "UiAddText", p, { successMessage: "Text block added" }) } };

const ueUiAddButton: ToolDefinition = { id: "ue_ui_add_button", name: "Add Button", description: "Add a Button widget with label", descriptionJa: "ラベル付きButtonウィジェットを追加", category: "ue_ui", inputSchema: z.object({ widgetName: z.string(), label: z.string().default("Button"), width: z.number().default(200), height: z.number().default(60) }), handler: async (p) => { return bridgeSendAsToolResult("unreal", "UiAddButton", p, { successMessage: (_, p) => `Button added: ${p.label}` }) } };

const ueUiAddImage: ToolDefinition = { id: "ue_ui_add_image", name: "Add Image", description: "Add an Image widget with texture", descriptionJa: "テクスチャ付きImageウィジェットを追加", category: "ue_ui", inputSchema: z.object({ widgetName: z.string(), texturePath: z.string(), width: z.number().default(100), height: z.number().default(100) }), handler: async (p) => { return bridgeSendAsToolResult("unreal", "UiAddImage", p, { successMessage: "Image added" }) } };

const ueUiAddProgressBar: ToolDefinition = { id: "ue_ui_add_progress_bar", name: "Add Progress Bar", description: "Add a ProgressBar widget", descriptionJa: "ProgressBarウィジェットを追加", category: "ue_ui", inputSchema: z.object({ widgetName: z.string(), percent: z.number().min(0).max(1).default(0.5), fillColor: z.string().default("0,1,0,1") }), handler: async (p) => { return bridgeSendAsToolResult("unreal", "UiAddProgressBar", p, { successMessage: "Progress bar added" }) } };

const ueUiShowWidget: ToolDefinition = { id: "ue_ui_show_widget", name: "Show/Hide Widget", description: "Add widget to viewport or remove it", descriptionJa: "ウィジェットをビューポートに追加/削除", category: "ue_ui", inputSchema: z.object({ widgetName: z.string(), visible: z.boolean().default(true) }), handler: async (p) => { return bridgeSendAsToolResult("unreal", "UiShowWidget", p, { successMessage: (_, p) => `Widget ${p.visible ? "shown" : "hidden"}` }) } };

export const ueUiTools: ToolDefinition[] = [ ueUiCreateWidget, ueUiAddText, ueUiAddButton, ueUiAddImage, ueUiAddProgressBar, ueUiShowWidget ];