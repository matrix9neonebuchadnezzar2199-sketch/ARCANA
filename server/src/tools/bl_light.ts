import { ToolDefinition } from "../core/registry";
import { bridge } from "../bridge";
import { z } from "zod";
const blLightCreate: ToolDefinition = { id: "bl_light_create", name: "Create Light", description: "Create a light (Point/Sun/Spot/Area)", descriptionJa: "ライトを作成（Point/Sun/Spot/Area）", category: "bl_light", inputSchema: z.object({ type: z.enum(["POINT","SUN","SPOT","AREA"]).default("POINT"), x: z.number().default(0), y: z.number().default(0), z: z.number().default(5) }), handler: async (p) => { try { const r = await bridge.send("blender", "bl_light_create", p); return r ? { success: true, message: `Light created: ${p.type}`, data: r } : { success: false, message: "Failed" }; } catch (error: any) { return { success: false, message: `Error: ${error.message}` }; } } };

const blLightSetColor: ToolDefinition = { id: "bl_light_set_color", name: "Set Light Color", description: "Set light color RGB", descriptionJa: "ライトの色をRGBで設定", category: "bl_light", inputSchema: z.object({ name: z.string(), r: z.number().default(1), g: z.number().default(1), b: z.number().default(1) }), handler: async (p) => { try { const r = await bridge.send("blender", "bl_light_set_color", p); return r ? { success: true, message: "Light color set", data: r } : { success: false, message: "Failed" }; } catch (error: any) { return { success: false, message: `Error: ${error.message}` }; } } };

const blLightSetPower: ToolDefinition = { id: "bl_light_set_power", name: "Set Light Power", description: "Set light power in watts", descriptionJa: "ライトの出力をワットで設定", category: "bl_light", inputSchema: z.object({ name: z.string(), power: z.number().default(1000) }), handler: async (p) => { try { const r = await bridge.send("blender", "bl_light_set_power", p); return r ? { success: true, message: `Power: ${p.power}W`, data: r } : { success: false, message: "Failed" }; } catch (error: any) { return { success: false, message: `Error: ${error.message}` }; } } };

const blLightSetShadow: ToolDefinition = { id: "bl_light_set_shadow", name: "Set Light Shadow", description: "Enable or disable shadow and set softness", descriptionJa: "影の有効/無効とソフトネスを設定", category: "bl_light", inputSchema: z.object({ name: z.string(), enable: z.boolean().default(true), softness: z.number().default(0.25) }), handler: async (p) => { try { const r = await bridge.send("blender", "bl_light_set_shadow", p); return r ? { success: true, message: `Shadow ${p.enable ? "on" : "off"}`, data: r } : { success: false, message: "Failed" }; } catch (error: any) { return { success: false, message: `Error: ${error.message}` }; } } };

const blLightSetSize: ToolDefinition = { id: "bl_light_set_size", name: "Set Light Size", description: "Set light source size (radius or area dimensions)", descriptionJa: "ライトソースのサイズを設定", category: "bl_light", inputSchema: z.object({ name: z.string(), size: z.number().default(0.25) }), handler: async (p) => { try { const r = await bridge.send("blender", "bl_light_set_size", p); return r ? { success: true, message: `Size: ${p.size}`, data: r } : { success: false, message: "Failed" }; } catch (error: any) { return { success: false, message: `Error: ${error.message}` }; } } };

const blLightSetType: ToolDefinition = { id: "bl_light_set_type", name: "Change Light Type", description: "Change light type (Point/Sun/Spot/Area)", descriptionJa: "ライトタイプを変更", category: "bl_light", inputSchema: z.object({ name: z.string(), type: z.enum(["POINT","SUN","SPOT","AREA"]) }), handler: async (p) => { try { const r = await bridge.send("blender", "bl_light_set_type", p); return r ? { success: true, message: `Type changed to ${p.type}`, data: r } : { success: false, message: "Failed" }; } catch (error: any) { return { success: false, message: `Error: ${error.message}` }; } } };

export const blLightTools: ToolDefinition[] = [ blLightCreate, blLightSetColor, blLightSetPower, blLightSetShadow, blLightSetSize, blLightSetType ];