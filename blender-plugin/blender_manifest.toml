schema_version = "1.0.0"

id = "arcana_bridge"
version = "6.0.0"
name = "ARCANA Bridge"
tagline = "Connect AI assistants to Blender via MCP"
maintainer = "ARCANA Project"
type = "add-on"

blender_version_min = "4.2.0"

[permissions]
network = "Connect to ARCANA MCP server via WebSocket on localhost:9879"
